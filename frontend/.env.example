# Vite Client Environment Configuration
# In local development with Vite proxy, leave empty or set to http://localhost:5000/api
# In production (e.g. deployed on Vercel), set to your live backend domain
VITE_API_URL=/api
